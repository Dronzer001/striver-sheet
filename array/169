class Solution {
public:
    int majorityElement(vector<int>& nums) {
        int n= nums.size();
        unordered_map<int,int>mid;
        for(int i=0;i<n;i+=1){
            mid[nums[i]]++;
            if(mid[nums[i]]>(n/2)) return nums[i];
        }
        return -1;
    }
};

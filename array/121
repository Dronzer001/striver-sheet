class Solution {
public:
    int maxProfit(vector<int>& prices) {
        int n=prices.size();
        int min1=prices[0],ans=0;
        for(int i=1;i<n;i+=1){
            ans=max(ans,prices[i]-min1);
            min1=min(min1,prices[i]);
        }
        return ans;
    }
};
